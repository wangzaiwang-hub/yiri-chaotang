# 快速执行皇帝毒舌日志系统迁移

## 🚀 最快的方法（3 步）

### 步骤 1：打开 Supabase 控制台

访问：https://supabase.com/dashboard

### 步骤 2：打开 SQL 编辑器

1. 选择你的项目
2. 点击左侧菜单 "SQL Editor"
3. 点击 "New Query"

### 步骤 3：复制并执行 SQL

复制 `database/add_emperor_sarcasm_logs_simple.sql` 中的所有代码，粘贴到 SQL 编辑器，点击 "Run"。

---

## ✅ 验证迁移成功

在 SQL 编辑器中执行以下查询：

```sql
-- 检查表
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('emperor_sarcasm_logs', 'minister_log_progress');

-- 检查函数
SELECT routine_name FROM information_schema.routines 
WHERE routine_schema = 'public' 
AND routine_name LIKE '%sarcasm%' OR routine_name LIKE '%log%';
```

如果看到以下结果，说明迁移成功：

```
表：
- emperor_sarcasm_logs
- minister_log_progress

函数：
- update_unviewed_count
- mark_logs_as_viewed
- get_unviewed_logs
- get_log_history
- create_sarcasm_log
```

---

## 🧪 测试迁移

### 创建测试日志

```sql
SELECT * FROM create_sarcasm_log(
  'test-court-id',
  'test-user-id',
  'task_assigned',
  '[14:30] 皇帝让大臣A去唱歌，大臣A连屁都不敢放，乖乖接受了',
  '{"taskId": "task-1", "taskName": "唱歌"}'::JSONB
);
```

### 查询未查看的日志

```sql
SELECT * FROM get_unviewed_logs('test-court-id', 'test-user-id', 10);
```

### 标记日志为已查看

```sql
-- 获取日志 ID
SELECT id FROM emperor_sarcasm_logs 
WHERE court_id = 'test-court-id' AND user_id = 'test-user-id'
LIMIT 1;

-- 标记为已查看（替换 log-id）
SELECT * FROM mark_logs_as_viewed(
  'test-court-id',
  'test-user-id',
  ARRAY['log-id']
);
```

---

## 📝 SQL 文件位置

- **完整版**：`database/add_emperor_sarcasm_logs.sql`
- **简化版**（推荐）：`database/add_emperor_sarcasm_logs_simple.sql`

---

## 🔄 回滚迁移

如果需要回滚，在 SQL 编辑器执行：

```sql
DROP TRIGGER IF EXISTS trigger_update_unviewed_count ON emperor_sarcasm_logs;
DROP FUNCTION IF EXISTS update_unviewed_count();
DROP FUNCTION IF EXISTS mark_logs_as_viewed(UUID, UUID, UUID[]);
DROP FUNCTION IF EXISTS get_unviewed_logs(UUID, UUID, INT);
DROP FUNCTION IF EXISTS get_log_history(UUID, UUID, INT, INT);
DROP FUNCTION IF EXISTS create_sarcasm_log(UUID, UUID, VARCHAR, TEXT, JSONB);
DROP VIEW IF EXISTS unviewed_logs_view;
DROP INDEX IF EXISTS idx_emperor_logs_court_user;
DROP INDEX IF EXISTS idx_emperor_logs_created_at;
DROP INDEX IF EXISTS idx_emperor_logs_viewed;
DROP INDEX IF EXISTS idx_minister_progress_court_user;
DROP TABLE IF EXISTS minister_log_progress;
DROP TABLE IF EXISTS emperor_sarcasm_logs;
```

---

## 📚 下一步

迁移完成后，需要：

1. 创建后端 API 端点
2. 创建前端日志弹窗
3. 集成游戏逻辑

详见 `docs/皇帝毒舌日志系统.md`
